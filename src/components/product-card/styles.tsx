import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  header: {
    fontWeight: "bold",
  },
  parapraph: {
    marginTop: "10px",
  },
  price: {
    marginTop: "10px",
    fontWeight: "bold",
  },
});
